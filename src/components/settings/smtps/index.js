import React, { Component } from 'react';
import { connect } from 'react-redux';

class SMTPs extends Component {

  render() {
    return(<div>SMTPs</div>)
  }
}

const mapStateToProps = ({}) => {
  return {};
};

export default connect(mapStateToProps, {})(SMTPs);
